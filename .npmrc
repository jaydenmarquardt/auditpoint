# SPFx/Heft resolve modules by walking a flat node_modules tree, so pnpm's
# default isolated store breaks the webpack + heft rigs. Hoisted mirrors npm.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
