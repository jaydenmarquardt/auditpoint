import { CatalogueEntry } from "./WebParts.types";
/** Component manifests installed on the web, used to name and icon each web part. */
export declare function WebPartCatalogue(webUrl?: string): {
    getAll(): Promise<CatalogueEntry[]>;
};
//# sourceMappingURL=WebPartCatalogue.api.d.ts.map