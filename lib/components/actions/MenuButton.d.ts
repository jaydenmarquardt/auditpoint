import * as React from "react";
import { MenuButtonProps } from "../Components.types";
/** Secondary actions folded behind one control, so a toolbar stays readable. */
export declare const MenuButton: React.FC<MenuButtonProps>;
//# sourceMappingURL=MenuButton.d.ts.map