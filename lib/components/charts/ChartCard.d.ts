import * as React from "react";
import { ChartCardProps } from "./Charts.types";
/**
 * Charts are the most expensive thing on an overview and their inputs rarely change
 * between renders, so identical points skip the redraw.
 */
export declare const ChartCard: React.NamedExoticComponent<ChartCardProps>;
//# sourceMappingURL=ChartCard.d.ts.map